# RiskApprove RAG Service - 100% FREE Dependencies
# No paid API libraries (OpenAI removed)

Flask==3.0.0
flask-cors==4.0.0

# LangChain for document processing (FREE)
langchain==0.1.0
langchain-community==0.0.10

# Vector store (FREE, runs locally)
faiss-cpu==1.7.4

# PDF parsing (FREE)
pypdf==3.17.0

# FREE HuggingFace embeddings (runs locally)
sentence-transformers>=2.3.0
torch>=2.1.0
transformers>=4.35.2
huggingface-hub>=0.20.0
